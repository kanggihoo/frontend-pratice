// ─── [타입 정의 파일] ──────────────────────────────────────────────────────────
// 이 파일에 앱에서 사용하는 모든 공용 타입을 정의하세요.
// completed/lib/types.ts를 참고해서 직접 작성해보세요.

// TODO: Post 인터페이스를 정의하세요.
// 포스트는 id(number), title(string), body(string), userId(number)를 가집니다.
// interface Post { ... }

// TODO: Comment 인터페이스를 정의하세요.
// 댓글은 id, postId, name, email, body (모두 알맞은 타입)을 가집니다.
// interface Comment { ... }

// TODO: ActionResult 인터페이스를 정의하세요.
// Server Action의 반환값: success(boolean), message(선택적 string)
// 힌트: 선택적 속성은 ? 를 붙입니다 → message?: string
// interface ActionResult { ... }
